{
  "name": "@secondcurrent/workflows",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "tsx src/index.ts",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@renderinc/sdk": "^0.6.0",
    "@secondcurrent/db": "workspace:*",
    "@secondcurrent/domain": "workspace:*",
    "@secondcurrent/integrations": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.2",
    "tsx": "^4.19.2",
    "vitest": "^3.0.0"
  }
}
